name: Release

on:
  push:
    branches:
      - develop-
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: sbp_90d92eb7477d035b493106a3d38e1793c9d88a68
      SUPABASE_DB_PASSWORD: IlWDMzB8pcALctm6
      PROJECT_ID: cimpzzikygouamvsjwxa

    steps:
      - uses: actions/checkout@v3

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase link --project-ref $PROJECT_ID
      - run: supabase db push