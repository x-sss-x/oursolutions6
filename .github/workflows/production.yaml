name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: sbp_14a856810e954c52b79897036c21de3a6b1fa8e7
      SUPABASE_DB_PASSWORD: 7HWtNPDWJYSmuYX7
      PROJECT_ID: jgcajpecsmkatlbzsstn

    steps:
      - uses: actions/checkout@v3

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase link --project-ref $PROJECT_ID
      - run: supabase db push