export default function Button() {
    return (
      <button className="text-secondary text-[24px] font-serif hover:underline focus:outline-none text-sky-700">
        forgot password?
      </button>
    );
  }